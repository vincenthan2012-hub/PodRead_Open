{
  "name": "podread-unified",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:server": "cd server && npm install",
    "build:all": "npm run build && npm run build:server",
    "start": "node server/index-unified.js",
    "start:unified": "npm run build && node server/index-unified.js",
    "preview": "npm run build && npm start",
    "dev:unified": "npm run build && node server/index-unified.js"
  },
  "dependencies": {
    "react": "^19.2.4",
    "@google/genai": "^1.40.0",
    "react-dom": "^19.2.4",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "nodemailer": "^6.9.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
